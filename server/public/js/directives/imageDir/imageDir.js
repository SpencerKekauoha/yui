//Use this directive to change the sizes of images in the frontend
//next, create an array of images in the back and send it to the frontend


// angular.module('yui')
//     .directive('imageSizing', function () {
//         return {
//             restrict: 'A',
//             link: function (scope, element, attribute) {
//                 element.find('image').css('width', '100px');
//             }
//             if(image[i] % 3) {
//               element.find('image').css('width', '100px');
//             }
//             else if(image[i] % 5) {
//               element.find('image').css('width', '100px');
//             }
//         };
//     });
